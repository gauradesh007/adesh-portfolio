<section class="hero">
  <div class="container hero-grid">
    <!-- Left column -->
    <div class="hero-content">
      
      <p class="person-name">Adesh Gaur</p>

      <h1>Enterprise Integration Architect</h1>
      <p class="specialization-label"> Specializing in </p>


      <p class="specialization">
        Boomi
        <span>•</span>
        Healthcare Integration &amp; EDI
        <span>•</span>
        Enterprise APIs
        <span>•</span>
        AI-powered Solutions
      </p>

      <p class="hero-copy">
        I help organizations modernize enterprise integration by designing
        scalable Boomi solutions, healthcare interoperability platforms,
        enterprise APIs, and intelligent AI-powered architectures. My
        experience spans more than two decades of enterprise engineering,
        including healthcare payer integration, EDI, solution architecture,
        and technical leadership.
      </p>

      <div class="button-row">
        <a class="button button-primary" href="#boomi">
          Explore Expertise
        </a>

        <a class="button button-secondary" href="/products">
          Featured Products
        </a>

        <a class="button button-secondary" href="/contact">
          Let&apos;s Connect
        </a>
      </div>
    </div>

    <!-- Right column -->
    <aside class="focus-panel" aria-labelledby="core-expertise-title">
      <p id="core-expertise-title" class="panel-label">
        Core Expertise
      </p>

      <ul>
        <li>
          <span class="marker" aria-hidden="true">01</span>

          <div>
            <strong>Enterprise Integration</strong>
            <small>
              Architecture, patterns, governance, and delivery
            </small>
          </div>
        </li>

        <li>
          <span class="marker" aria-hidden="true">02</span>

          <div>
            <strong>Boomi Architecture</strong>
            <small>
              Integration, APIs, Event Streams, and solution design
            </small>
          </div>
        </li>

        <li>
          <span class="marker" aria-hidden="true">03</span>

          <div>
            <strong>Healthcare Integration &amp; EDI</strong>
            <small>
              834, 837, 835, 820, HL7, validation, mapping, routing,
              and automation
            </small>
          </div>
        </li>

        <li>
          <span class="marker" aria-hidden="true">04</span>

          <div>
            <strong>Enterprise APIs</strong>
            <small>
              API design, API management, security, and governance
            </small>
          </div>
        </li>

        <li>
          <span class="marker" aria-hidden="true">05</span>

          <div>
            <strong>Technical Leadership &amp; AI</strong>
            <small>
              Architecture reviews, estimation, Agentic AI, RAG,
              and automation
            </small>
          </div>
        </li>
      </ul>
    </aside>
  </div>

  <div class="container credibility-strip" aria-label="Professional highlights">
    <div>
      <strong>20+</strong>
      <span>Years in enterprise technology</span>
    </div>

    <div>
      <strong>7+</strong>
      <span>Years in healthcare integration</span>
    </div>

    <div>
      <strong>6</strong>
      <span>Verified Boomi credentials</span>
    </div>

    <div>
      <strong>Live</strong>
      <span>Production AI application</span>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 5rem 0 3rem;
    background:
      radial-gradient(
        circle at 85% 15%,
        rgba(22, 95, 184, 0.11),
        transparent 28%
      ),
      linear-gradient(180deg, #ffffff, #f7fafe);
  }

  .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.35fr) minmax(340px, 0.7fr);
    gap: 4.5rem;
    align-items: start;
  }

  .person-name {
      margin: 0 0 0.3rem;
      color: #0f2240;        /* Darkest color */
      font-size: 5.2rem;
      font-weight: 900;
      letter-spacing: -0.03em;
      line-height: 1.5;
    }

    .specialization-label {
      margin: 0 0 0.5rem;
      color: var(--color-text-muted);
      font-size: 0.95rem;
      font-weight: 600;
      letter-spacing: 0.02em;
      text-transform: none;
    }

    .specialization {
      display: flex;
      flex-wrap: wrap;
      gap: 0.55rem;
      margin: 0 0 1.75rem;
      color: var(--color-text);
      font-size: 1rem;
      font-weight: 700;
    }

  .hero h1 {
      max-width: 760px;
      margin: 0 0 1rem;

      font-size: clamp(1.4rem, 3vw, 1.7rem);
      font-weight: 600;

      color: #243b63;   /* <-- slightly lighter than the name */

      line-height: 1.05;
      letter-spacing: -0.045em;
    }

  .specialization {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
    margin: 0 0 1.5rem;
    color: var(--color-text);
    font-size: 1rem;
    font-weight: 750;
  }

  .specialization span {
    color: var(--color-primary);
  }

  .hero-copy {
    max-width: 720px;
    margin: 0 0 2rem;
    color: var(--color-text-body);
    font-size: 1.12rem;
    line-height: 1.75;
  }

  .focus-panel {
    padding: 1.6rem;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    background: rgba(255, 255, 255, 0.94);
    box-shadow: var(--shadow-md);
  }

  .panel-label {
    margin: 0 0 1.25rem;
    color: var(--color-primary);
    font-size: 0.78rem;
    font-weight: 850;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .focus-panel ul {
    display: grid;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .focus-panel li {
    display: grid;
    grid-template-columns: 2.5rem minmax(0, 1fr);
    gap: 0.85rem;
    align-items: start;
    padding: 1rem 0;
    border-bottom: 1px solid var(--color-border);
  }

  .focus-panel li:last-child {
    border-bottom: 0;
  }

  .focus-panel li > div {
    display: grid;
    gap: 0.2rem;
    min-width: 0;
  }

  .focus-panel strong {
    color: var(--color-text);
    line-height: 1.35;
  }

  .focus-panel small {
    color: var(--color-text-muted);
    line-height: 1.45;
  }

  .credibility-strip {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    margin-top: 4.5rem;
    overflow: hidden;
    border-radius: var(--radius-md);
    background: var(--color-text);
  }

  .credibility-strip > div {
    display: grid;
    gap: 0.1rem;
    padding: 1.35rem;
    border-right: 1px solid rgba(255, 255, 255, 0.13);
  }

  .credibility-strip > div:last-child {
    border-right: 0;
  }

  .credibility-strip strong {
    color: #ffffff;
    font-size: 1.45rem;
    line-height: 1.2;
  }

  .credibility-strip span {
    color: #cbd5e1;
    font-size: 0.83rem;
    line-height: 1.45;
  }

  @media (max-width: 900px) {
    .hero {
      padding-top: 4.5rem;
    }

    .hero-grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .focus-panel {
      max-width: 42rem;
    }

    .credibility-strip {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .credibility-strip > div:nth-child(2) {
      border-right: 0;
    }

    .credibility-strip > div:nth-child(-n + 2) {
      border-bottom: 1px solid rgba(255, 255, 255, 0.13);
    }
  }

  @media (max-width: 560px) {
    .hero {
      padding-top: 3.75rem;
    }

    .specialization {
      gap: 0.4rem;
      font-size: 0.94rem;
    }

    .hero-copy {
      font-size: 1rem;
    }

    .focus-panel {
      padding: 1.25rem;
    }

    .credibility-strip {
      grid-template-columns: 1fr;
    }

    .credibility-strip > div {
      border-right: 0;
      border-bottom: 1px solid rgba(255, 255, 255, 0.13);
    }

    .credibility-strip > div:last-child {
      border-bottom: 0;
    }
  }
</style>